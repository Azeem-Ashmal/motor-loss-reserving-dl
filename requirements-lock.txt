# Full transitive dependency closure of requirements.txt, resolved from
# actually-installed package metadata (importlib.metadata + packaging,
# walking each of numpy/pandas/scikit-learn/torch/chainladder/PyYAML/
# matplotlib/scipy's real Requires-Dist entries) rather than a raw
# `pip freeze` of the host machine's Python installation. An earlier version
# of this file was exactly that raw freeze, taken with no virtual
# environment active, and consequently included ~75 entries that are Ubuntu
# OS-level or desktop packages with no relationship to this project at all
# (aptdaemon, bcc, systemd-python, ubuntu-pro-client, PyGObject, and
# similar) - a real reproducibility-hygiene bug, not a stylistic nitpick,
# since it materially misrepresented what this pipeline actually depends on.
#
# Python interpreter used (not a pip package, kept as a comment so this file
# still parses as valid pip requirements):
# Python 3.14.4
chainladder==0.10.0
contourpy==1.3.3
cycler==0.12.1
dill==0.4.1
filelock==3.32.3
fonttools==4.63.0
fsspec==2026.7.0
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
llvmlite==0.48.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
narwhals==2.24.0
networkx==3.6.1
numba==0.66.0
numpy==2.4.6
packaging==26.3
pandas==3.0.3
patsy==1.0.2
pillow==12.1.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.18.0
setuptools==78.1.1
six==1.17.0
sparse==0.19.1
sympy==1.14.0
threadpoolctl==3.6.0
torch==2.13.0+cpu
typing-extensions==4.15.0
